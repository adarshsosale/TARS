# Isaac Lab 2.3.2 / Isaac Sim 5.1.0 / PyTorch 2.7.0+cu128
# For NVIDIA RTX PRO 6000 Blackwell (sm_120). Python 3.11 ONLY.
#
# This file installs the pip-resolvable layer. Isaac Lab itself is installed
# from source in a later step — see INSTALL.md for the full procedure.

--extra-index-url https://download.pytorch.org/whl/cu128
--extra-index-url https://pypi.nvidia.com

# Pin setuptools < 81 so pkg_resources stays available.
# Needed because flatdict 4.0.1 (a transitive Isaac Lab dep) has a legacy
# setup.py that imports pkg_resources, removed in setuptools 81+.
setuptools<81

# PyTorch with CUDA 12.8 — the explicit +cu128 local version forces pip
# to the cu128 index. Required for sm_120 (Blackwell) kernel support.
torch==2.7.0+cu128
torchvision==0.22.0+cu128

# Isaac Sim 5.1.0 (pulled from pypi.nvidia.com).
# 5.x is required for sm_120; 4.x emits "unsupported compute capability".
isaacsim[all,extscache]==5.1.0

# Diagnostics — used to inspect stuck Python processes (e.g. shutdown hangs).
py-spy
